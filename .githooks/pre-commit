#!/bin/sh
# Pre-commit hook: keep formatting and lints clean before each commit.
set -e
cargo fmt --all -- --check || {
  echo "rustfmt found issues. Run 'cargo fmt --all' and stage the result." >&2
  exit 1
}
cargo clippy --workspace --all-targets --quiet -- -D warnings
